<%= form_for @recipe do |f| %>
<%= f.label :name %>
<%= f.text_field :name %> <br>
<% Ingredient.all.each do |ingredient| %>
<input type="checkbox" id="<%= ingredient.name %>" name="ingredient[]" value="<%= ingredient.id %>" /> <%= ingredient.name %> <br>
<% end %>
<%= f.submit %>
<% end %>
